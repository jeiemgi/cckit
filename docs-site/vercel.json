{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "buildCommand": "astro build",
  "outputDirectory": "dist",
  "ignoreCommand": "bash -c '[ \"${VERCEL_GIT_COMMIT_MESSAGE#chore(release):}\" != \"$VERCEL_GIT_COMMIT_MESSAGE\" ] && exit 1 || exit 0'"
}
