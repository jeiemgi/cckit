---
title: How to stop Claude Code from committing secrets
description: Wire cckit's built-in secret guard as a pre-commit hook so keys, .env files, and your private data can never be committed — by you or by Claude Code.
tutorial:
  job: stop Claude Code from committing secrets
  difficulty: beginner
  time: "5 min"
  prerequisites: [git, "cckit installed and initialized"]
  ogTag: Security
  keywords: [claude code secrets, stop ai committing secrets, pre-commit ai guard]
  related: [private-data-out-of-ai-commits, run-checks-with-hooks, control-claude-permissions]
---

import { Steps, Aside, CardGrid, LinkCard } from '@astrojs/starlight/components';
import TagRow from '../../../components/TagRow.astro';
import TutorialMeta from '../../../components/TutorialMeta.astro';
import TutorialSchema from '../../../components/TutorialSchema.astro';

<TagRow kinds={["concept", "hook"]} />

<TutorialMeta />

**What you'll set up:** cckit ships a secret guard that scans every staged change for keys, `.env`
files, private-key blocks, and your own private terms. Turn it into a git **pre-commit hook** and the
commit is blocked the moment a secret is staged — whether you typed it or Claude Code did. It takes
one command.

## Steps

<Steps>

1. **Turn on the pre-commit hook.** cckit ships the hook in `githooks/`; point git at it once.

   <div class="cckit-prompt">

   ```bash frame="terminal"
   git config core.hooksPath githooks
   ```

   </div>

   From now on git runs cckit's guard on every `git commit`, before anything reaches a remote.

2. **Prove it blocks a secret.** Stage a fake `.env` and try to commit — the guard fails the commit.

   ```bash frame="terminal"
   echo "API_KEY=sk-not-a-real-key" > .env
   git add -f .env && git commit -m "test"
   # ✗ blocked: forbidden file .env — commit aborted
   ```

   Nothing was committed. Delete the test file: `rm .env`.

3. **Declare your own private terms (optional).** Beyond keys and env files, add anything private to
   you — org names, internal hosts, emails — one per line.

   ```bash frame="terminal"
   cp privacy-denylist.example .cckit/privacy-denylist   # the target is gitignored
   # …add your terms, one per line…
   ```

   The guard now also fails a commit if any of those terms appears in a tracked file.

</Steps>

## How it works

The guard checks code, docs, the cookbook, examples, and templates — every commit, every time. It
runs in two places: the local check suite (`scripts/check.sh`) and the pre-commit hook you just
enabled. A finding blocks the commit; it is not optional and is not waved through by permission
consent. Because it fires at commit time, a leaked key is caught **before** it can reach a remote,
so Claude Code cannot commit a secret even when it's driving git for you.

<Aside type="note" title="Why a pre-commit hook">
A pre-commit hook is a script git runs the instant you commit. If the script exits non-zero, the
commit is blocked — which is exactly the moment to stop a secret, before it leaves your machine.
</Aside>

## Troubleshooting

**The guard didn't run on my commit.** The hook path isn't set. Re-run
`git config core.hooksPath githooks` in the repo, and confirm with `git config core.hooksPath`.

**It blocked a placeholder that isn't a real secret.** Placeholders like `<...>`, `${...}`, and
`YOUR_…` are allowed by design. If a real placeholder still trips it, rephrase it to one of those
forms rather than disabling the guard.

**My teammate's clone doesn't enforce it.** `core.hooksPath` is per-clone local config. Each person
runs the one-time command once, or you wire it in your setup script — the hook itself is committed in
`githooks/`.

## Related tutorials

<CardGrid>
  <LinkCard title="Keep private & company data out of AI commits" href="/tutorials/keep-private-data-out-of-ai-commits/" description="Add your own denylist so private terms never get committed." />
  <LinkCard title="Run checks automatically with cckit hooks" href="/tutorials/run-checks-with-cckit-hooks/" description="What runs at commit time and how to extend it." />
  <LinkCard title="Control what Claude Code is allowed to do" href="/tutorials/control-what-claude-code-can-do/" description="The permission model around the guard." />
</CardGrid>

<TutorialSchema
  steps={[
    { name: 'Turn on the pre-commit hook', text: 'Point git at cckit\'s bundled hooks with: git config core.hooksPath githooks. Git now runs the secret guard on every commit.' },
    { name: 'Prove it blocks a secret', text: 'Stage a fake .env file and attempt a commit; the guard fails the commit before anything reaches a remote.' },
    { name: 'Declare your own private terms', text: 'Copy privacy-denylist.example to .cckit/privacy-denylist and list org names, hosts, or emails. The guard then blocks any commit containing those terms.' },
  ]}
  faqs={[
    { q: "The guard didn't run on my commit", a: 'The hook path is not set. Run git config core.hooksPath githooks in the repo and confirm with git config core.hooksPath.' },
    { q: 'It blocked a placeholder that is not a real secret', a: 'Placeholders like <...>, ${...}, and YOUR_… are allowed. Rephrase a real placeholder to one of those forms rather than disabling the guard.' },
    { q: "My teammate's clone doesn't enforce it", a: 'core.hooksPath is per-clone local config. Each person runs the one-time command once, or wire it into your setup script; the hook itself is committed in githooks/.' },
  ]}
/>
