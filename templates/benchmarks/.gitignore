# Benchmark runtime outputs — regenerated on every run, not source of truth.
.stop-state.json
.runs/
# RESULTS.md is regenerated by `cckit bench report`; untrack it by default. Delete this line if you
# want to commit the latest report as a record.
RESULTS.md
