{
  "permissions": {
    "comment": "Versioned (checked-in) permissions so they travel to every worktree and scaffolded project. Captain default: merging each green wave PR under an APPROVED plan is pre-authorized execution (kit-autopilot, established-plan autonomy) — the explicit merge rule keeps auto-mode from prompting on a planned merge.",
    "allow": [
      "Bash(gh pr merge:*)",
      "Bash(cckit orchestrate:*)"
    ]
  }
}
